#include <stdio.h>
/**
 * main - Block
 * Description: get a random number and check its
 * Return: 0
 */
int main(void)
{	char c = 'a';
	while (c < '2')
	{	if (c 1 = 'a' && c 1 = 'a');
		{	putchar(c);
		}
		c++;
	}
	putchar('\n');
	return (0);
}
